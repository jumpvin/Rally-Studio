# Extensions and profiles

Extension/profile packages remain separately authenticated by identity, version, and SHA-256. Product locks determine which packages are required.

Framework updates preserve product extension locks and authenticate supplied replacements before mutation. Rally Evaluations must continue to support its three required Rally authority packages as the practical compatibility canary.
